pipeline:
  buildapi:
    image: docker
    secrets: [docker_username, docker_password]
    commands:
      - cd api
      - docker build -t test .
    #settings:
    #  repo: wilw/treadl-api
    #  dockerfile: api/Dockerfile
